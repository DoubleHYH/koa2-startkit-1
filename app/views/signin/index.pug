extends /layout/default.pug

block title
    title 注册页

block content
    .container
        .row
            .col-sm-6.col-sm-offset-3
                h1= content
                form(method="POST")
                    .form-group
                        label(for="signin-user") username
                        input#signin-username.form-control(placeholder="user" type="text" name="username")
                    .form-group
                        label(for="signin-email") email
                        input#signin-email.form-control(placeholder="email" type="email" name="email")
                    .form-group
                        label(for="signin-user") password
                        input#signin-password.form-control(placeholder="password" type="password" name="password")
                    button.btn.btn-primary.btn-block(type="submit") 注册
